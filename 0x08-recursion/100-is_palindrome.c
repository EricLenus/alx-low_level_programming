#include"main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * return: always 0
 */

int main(void)
{
	int r;
	int l, k;

	for(l = 0, j = r - 1; l < k; l++, j--)
		if (str[l] != str[k]){
			return 0;
		}

return 1;
}
