#include"main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * return: always 0
 */
int is_palidrome(char *s)
	int len = strlen(str);
	int l, k;

	for(l = 0, j = len - 1; l < k; itt, j--) {
		if (str[l] != str[k]){
			return 0;
		}
	}
return 1;
}
